name: homl3
channels:
  - apple
  - conda-forge
  - defaults
dependencies:
  - bzip2=1.0.8
  - c-ares=1.18.1
  - ca-certificates=2022.12.7
  - cached-property=1.5.2
  - cached_property=1.5.2
  - grpcio=1.51.1  # [AMIT: version]
  - h5py=3.6.0
  - hdf5=1.12.1
  - krb5=1.19.3
  - libblas=3.9.0
  - libcblas=3.9.0
  - libcurl=7.86.0
  - libcxx=14.0.6
  - libedit=3.1.20191231
  - libev=4.33
  - libffi=3.4.2
  - libgfortran=5.0.0
  - libgfortran5=11.3.0
  - liblapack=3.9.0
  - libnghttp2=1.47.0
  - libopenblas=0.3.21
  - libprotobuf=3.19.4
  - libsqlite=3.40.0
  - libssh2=1.10.0
  - libzlib=1.2.13
  - llvm-openmp=15.0.6
  - ncurses=6.3
  - numpy=1.23.2      # Powerful n-dimensional arrays and numerical computing tools
  - openssl=3.0.7
  - pip           # Python's package-management system
  - protobuf
  - python=3.10.8  # your beloved programming language! :)
  - python_abi=3.10
  - readline=8.1.2
  - setuptools=65.5.1
  - six=1.16.0
  - tensorflow-deps
  - tk=8.6.12
  - tzdata=2022g
  - wheel         # built-package format for pip
  - xz=5.2.6
  - zlib=1.2.13
  - box2d-py=2.3  # used only in chapter 18, exercise 8
  - ffmpeg=5.1  # used only in the matplotlib tutorial to generate animations
  - graphviz  # used only in chapter 6 for dot files
  - python-graphviz  # used only in chapter 6 for dot files
  - ipython=8.5  # a powerful Python shell
  - ipywidgets=8.0  # optionally used only in chapter 12 for tqdm in Jupyter
  - joblib=1.1  # used only in chapter 2 to save/load Scikit-Learn models
  - jupyterlab=3.4  # to edit and run Jupyter notebooks
  - matplotlib=3.5  # beautiful plots. See tutorial tools_matplotlib.ipynb
  - nbdime=3.1  # optional tool to diff Jupyter notebooks
  - nltk=3.6  # optionally used in chapter 3, exercise 4
  - numexpr=2.8  # used only in the Pandas tutorial for numerical expressions
  - pandas=1.4  # data analysis and manipulation tool
  - pillow=9.2  # image manipulation library, (used by matplotlib.image.imread)
  - py-xgboost=1.6  # used only in chapter 6 for optimized Gradient Boosting
  - pydot=1.4  # used only for in chapter 10 for tf.keras.common.plot_model()
  - python=3.10  # your beloved programming language! :)
  - requests=2.28  # used only in chapter 19 for REST API queries
  - scikit-learn=1.1  # machine learning library
  - scipy=1.9  # scientific/technical computing library
  - statsmodels=0.13  # used only in chapter 15 for time series analysis
  - tqdm=4.64  # used only in chapter 12 to display nice progress bars
  - widgetsnbextension=4.0  # interactive HTML widgets for Jupyter notebooks
  - pip:
      - absl-py==1.3.0
      - astunparse==1.6.3
      - cachetools==5.2.0
      - certifi==2022.12.7
      - charset-normalizer==2.1.1
      - flatbuffers==22.12.6
      - gast==0.4.0
      - google-auth==2.15.0
      - google-auth-oauthlib==0.4.6
      - google-pasta==0.2.0
      - idna==3.4
      - keras==2.11.0
      - libclang==14.0.6
      - markdown==3.4.1
      - markupsafe==2.1.1
      - oauthlib==3.2.2
      - opt-einsum==3.3.0
      - packaging~=21.0  #  [AMIT: version]
      - pyasn1==0.4.8
      - pyasn1-modules==0.2.8
      - requests==2.28.1
      - requests-oauthlib==1.3.1
      - rsa==4.9
      - tensorboard==2.11.0             # TensorFlow's visualization toolkit
      - tensorboard-data-server==0.6.1
      - tensorboard-plugin-wit==1.8.1
      - tensorflow-estimator==2.11.0
      - tensorflow-macos==2.11.0
      - tensorflow-metal==0.7.0
      - termcolor==2.1.1
      - typing-extensions
      - urllib3==1.26.13
      - werkzeug==2.2.2
      - wrapt==1.14.1
      - keras-tuner~=1.1.3  # used in chapters 10 and 19 for hyperparameter tuning
      - tensorboard-plugin-profile~=2.11.1  # profiling plugin for TensorBoard [AMIT: changed version]
      - tensorflow-addons~=0.19.0  # used in chapters 11 & 16 (for AdamW & seq2seq) [AMIT: changed version]
      - tensorflow-datasets~=4.7.0  # datasets repository, ready to use [AMIT: changed version]
      - tensorflow-hub~=0.12.0  # trained ML models repository, ready to use
      - tensorflow-serving-api~=2.10.0  # or tensorflow-serving-api-gpu if gpu
      - transformers~=4.25.0  # Natural Language Processing lib for TF or PyTorch [AMIT: version]
      - urlextract~=1.7.0  # optionally used in chapter 3, exercise 4  [AMIT: version]
      - gym[classic_control,atari,accept-rom-license]~=0.26.1  # used only in ch18
      - google-cloud-aiplatform~=1.19.0  # used only in chapter 19  [AMIT: version]
      - google-cloud-storage~=2.7.0  # used only in chapter 19  [AMIT: version]
prefix: /opt/homebrew/Caskroom/miniconda/base/envs/homl3